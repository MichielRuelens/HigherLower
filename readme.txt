# NPM Package manager defines the following scripts
"dev": "webpack-dev-server --mode=development",
"build": "webpack --mode=production"

# Make sure you are in the frontend folder (where package.json is located):
cd <project>/frontend

# Then run the scripts as:
npm run dev
npm run build

# Running the backend API
venv/Scripts/python run/run_api.py
